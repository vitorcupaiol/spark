# This is a Databricks asset bundle definition for elt_uber_eats_analytics_owshq.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: elt_uber_eats_analytics_owshq
  uuid: 076ae738-7bb8-4610-b93a-cf650bc35859

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-2090585310411504.4.azuredatabricks.net

  prod:
    mode: production
    workspace:
      host: https://adb-2090585310411504.4.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/luanmorenomaciel@hotmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/luanmorenomaciel@hotmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: luanmorenomaciel@hotmail.com
        level: CAN_MANAGE
